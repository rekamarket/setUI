import type {
  ButtonThemeType,
  ButtonSizeType,
  ButtonVariantType,
} from '../../theme'

export type Props = {
  href: string
  theme?: ButtonThemeType
  size?: ButtonSizeType
  variant?: ButtonVariantType
}
