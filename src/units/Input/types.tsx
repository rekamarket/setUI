import type {
  InputThemeType,
  InputSizeType,
  InputVariantType,
} from '../../theme'

export type Props = {
  theme?: InputThemeType
  size?: InputSizeType
  variant?: InputVariantType
  disabled?: boolean
}
