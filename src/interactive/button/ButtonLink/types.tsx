import type {
  ButtonThemeType,
  ButtonSizeType,
  ButtonVariantType,
} from '../css'

export type Props = {
  href: string
  theme?: ButtonThemeType
  size?: ButtonSizeType
  variant?: ButtonVariantType
}
