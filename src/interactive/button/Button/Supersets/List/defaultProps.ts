import type { Props } from './types'

export const defaultProps: Partial<Props> = {
  alignItems: 'stretch',
  borderRadius: 'pill',
  width: 'minContent',
  dividerColor: 'black',
  dividerWeight: 'thin',
}
